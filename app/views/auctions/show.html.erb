<h1><%= @auction.name %></h1>

<p>Ends at <%= @auction.ends_at %></p>

<div>
  <%= link_to "Edit this auction", edit_auction_path(@auction) %> |
  <%= link_to "Back to auctions", auctions_path %>

  <%= button_to "Destroy this auction", @auction, method: :delete, class: 'btn btn-danger' %>
</div>
